// Vars
import {
    mainContent, userForm, toggleProfile
} from '../config/vars';

// Global functions
import {
    toggleClass
} from '../modules/global_functions';

// Local Storage
import setNewPlayer from '../modules/local_storage';

export {
    mainContent,
    userForm,
    toggleProfile,
    toggleClass,
    setNewPlayer
}